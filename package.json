{
  "name": "mokkan_app",
  "version": "0.1.0",
  "private": true,
  "description": "Combined workspace to run the Rust backend and frontend together",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c auto \"cargo run --manifest-path backend/Cargo.toml\" \"pnpm -C frontend dev\"",
    "dev:backend": "cargo run --manifest-path backend/Cargo.toml",
    "dev:frontend": "pnpm -C frontend dev",
    "build": "pnpm -C frontend build && cargo build --manifest-path backend/Cargo.toml --release"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "packageManager": "pnpm@10.19.0"
}